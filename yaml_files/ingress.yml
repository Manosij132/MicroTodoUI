kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: todouiingress
spec:
  rules:
    - host: ui.mano.com
      http:
        paths:
          - backend:
              service:
                name: todouiservice
                port:
                  number: 80
            path: "/"
            pathType: Prefix
  ingressClassName: nginx

